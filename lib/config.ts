export const config = {
  // Backend API configuration
  API_BASE_URL: process.env.NEXT_PUBLIC_API_URL || 'http://localhost:5000',
  
  // App configuration
  APP_NAME: 'Site Builder',
  APP_DESCRIPTION: 'Build custom websites with HTML, CSS, and JavaScript',
}

export default config
